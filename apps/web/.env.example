# Browserbase — powers the emulator's "remote" lenses (third-party sites that
# block iframe embedding). Create a project at browserbase.com and copy both.
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
